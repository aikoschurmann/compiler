fn add(a: i32, b: i32) -> i32 { 
    return a + b; 
}

fn main() { 
    x: i32 = 0; 
    arr: i32[10]*;
    callback: fn(i32) -> i32;
    for (i: i32 = 0; i < 10; i = i + 1) { 
        x += i; 
    } 
}
